{
  "name": "drumnotes",
  "version": "1.0.0",
  "private": true,
  "description": "DrumNotes - iOS wrapper (Capacitor)",
  "scripts": {
    "sync": "bash sync-web.sh && npx cap sync ios",
    "ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
